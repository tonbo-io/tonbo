regression:
  enabled: true
  default:
    ops_per_sec: -0.15
    bytes_per_sec: -0.15
    wall_time_ms: +0.20
  per_benchmark:
    sequential_write:
      ops_per_sec: -0.12
      wall_time_ms: +0.20
    mixed:
      ops_per_sec: -0.12
      wall_time_ms: +0.25
    compaction:
      ops_per_sec: -0.12
      wall_time_ms: +0.25
      bytes_per_sec: -0.12
